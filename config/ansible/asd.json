{
    "builders": [{
    "type": "amazon-ebs",
    "region": "sa-east-1",
    "source_ami": "ami-0123456789abcdef",
    "instance_type": "t3.micro",
    "ssh_username": "ubuntu",
    "ami_name": "k8s-base-image-{{timestamp}}"
    }],
    "provisioners": [{
    "type": "ansible",
    "playbook_file": "playbooks/configure-base.yml"
    }]
}