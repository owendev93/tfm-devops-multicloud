{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "tfm-app.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "tfm-app.labels" . | nindent 4 }}
{{- end -}}
