apiVersion: v1
kind: Service
metadata:
  name: {{ include "tfm-app.fullname" . }}
  labels:
    {{- include "tfm-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "tfm-app.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
